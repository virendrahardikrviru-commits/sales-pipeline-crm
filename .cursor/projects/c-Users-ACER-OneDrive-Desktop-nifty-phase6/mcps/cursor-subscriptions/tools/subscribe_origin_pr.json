{
  "name": "subscribe_origin_pr",
  "description": "Subscribe this conversation to future Origin pull request events. Not delivered: PR lifecycle events that echo this agent's OWN actions — creator-actored events on this agent's own working branch (its own pushes/synchronize, closes; Origin agents act as their owner) — and metadata-only edits (title/body/base changes). Always delivered: all comments and reviews — including Bugbot and Agentic Security Review activity, which posts from app identities — plus lifecycle events actored by third parties or on any other branch. After subscribing, state what you are waiting for and end your turn; the event arrives as a follow-up notification in this conversation.",
  "arguments": {
    "type": "object",
    "properties": {
      "options": {
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "scope": {
                "const": "pr"
              },
              "prUrl": {
                "type": "string",
                "description": "An Origin pull request URL (https://cursor.com/codebase/<owner>/<repo>/pull/<n>)."
              }
            },
            "required": [
              "scope",
              "prUrl"
            ]
          },
          {
            "type": "object",
            "properties": {
              "scope": {
                "const": "pr"
              },
              "repo": {
                "type": "string"
              },
              "pullNumber": {
                "type": "number"
              }
            },
            "required": [
              "scope",
              "repo",
              "pullNumber"
            ]
          },
          {
            "type": "object",
            "properties": {
              "scope": {
                "const": "repo"
              },
              "repo": {
                "type": "string"
              }
            },
            "required": [
              "scope",
              "repo"
            ]
          }
        ]
      },
      "expiresInSeconds": {
        "type": "number"
      }
    },
    "required": [
      "options"
    ]
  }
}